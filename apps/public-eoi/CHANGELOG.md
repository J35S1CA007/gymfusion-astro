# Change Log

## Unreleased
- Reset mobile and desktop accessibility settings on refresh, defaulted reduced motion to off, paused desktop marquees when enabled, and repaired desktop contrast, spacing, dyslexia font, and bounded text scaling.
- Switched the mobile dyslexia accessibility font to the `open-dyslexic` npm package served by jsDelivr without changing desktop behavior.
- Widened and solidified the mobile accessibility panel, restored five text-size levels, and corrected its contrast, spacing, and reduced-motion controls without changing desktop behavior.
- Shifted the mobile form message pills 20px left without changing desktop behavior.
- Refined the mobile form toolbar and accessibility-control insets to prevent edge clipping without changing desktop behavior.
- Hid only the in-form mobile menu icon artwork while preserving the form structure, spacing, and desktop behavior.
- Added a mobile-only Public EOI header treatment with a dedicated member button, centered spiral mark, and animated menu overlay while leaving desktop header behavior intact.
- Suppressed the legacy cover-page in-form mobile menu icon while retaining the step toolbar icons.
- Removed the cover-page mobile form menu button from the rendered mobile EOI markup.
- Removed the start-page mobile menu button from the Public EOI mobile form view without changing desktop behavior.
- Tightened the mobile cover-page `START NOW` glow and added a mobile-only breathing pulse without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA down 4px without changing desktop behavior.
- Increased the mobile cover-page `START NOW` CTA button size by 10px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA up another 30px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA up another 10px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA up another 15px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA up another 10px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA up another 5px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA up another 50px without changing desktop behavior.
- Moved the mobile cover-page `START NOW` CTA 20px higher without changing desktop behavior.
- Increased the mobile cover overlay image by another 5px on the Public EOI start screen without changing desktop behavior.
- Increased the mobile cover overlay image by another 4px on the Public EOI start screen without changing desktop behavior.
- Increased the mobile cover overlay image by 3px on the Public EOI start screen without changing desktop behavior.
- Refined the mobile-only member/login control to match the current Wix mobile header SVG treatment, glow, spacing, reduced-size presentation, vertical alignment, and slightly larger mobile sizing without changing desktop layout.
- Further adjusted the mobile Login control placement on the Public EOI mobile header by moving it 1px right, without changing the mobile strip height or desktop layout.
- Adjusted the mobile Login control scale back to `1` on the Public EOI mobile header by honoring the host scale value, without changing desktop layout or header strip height.
- Decreased the mobile spiral logo size by 2px on the Public EOI mobile header without changing the desktop logo rule.
- Moved the mobile menu button 3px left on the Public EOI mobile header without changing the desktop menu button rule.
- Moved the mobile menu item stack up by 20px on the Public EOI mobile overlay without changing desktop behavior.
- Moved the mobile menu item stack up by another 50px on the Public EOI mobile overlay without changing desktop behavior.
- Moved the mobile menu item stack up by another 20px on the Public EOI mobile overlay without changing desktop behavior.
- Moved the mobile menu item stack up by another 80px on the Public EOI mobile overlay without changing desktop behavior.
- Moved the mobile menu item stack down by another 10px on the Public EOI mobile overlay without changing desktop behavior.
- Set the mobile menu item font weight to `500` on the Public EOI mobile overlay, slightly closer to regular, without changing desktop behavior.
- Moved the mobile canvas frame up by 3px on the Public EOI mobile page without changing desktop behavior.
- Shortened the mobile gradient frame by 3px at the bottom on the Public EOI mobile page without changing desktop behavior.

## v0.1.0
- Preserved the completed responsive Public EOI Phase 2 frontend as the first official `gymfusion-astro` app baseline.
- Retained the existing adult and truthful under-18 frontend behaviour without adding backend integration or CAPTCHA.
